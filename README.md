# distributional-semantics-word-vectors

Builds distributional word vectors from scratch using co-occurrence matrices, PPMI weighting, and SVD dimensionality reduction. Evaluates semantic representations on the SAT word analogy benchmark using Google News word2vec embeddings, comparing classical and neural approaches to capturing lexical relationships.

